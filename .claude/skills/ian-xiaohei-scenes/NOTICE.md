# Notice

Ian Xiaohei Scenes is an AI-agent skill created by Ian.

## Local modifications in this repository

This is a vendored copy of <https://github.com/helloianneo/ian-xiaohei-scenes>
(MIT), adapted for the `reels` pipeline. Changes made:

- Added a "LOCAL OVERRIDES" section at the top of `SKILL.md`: the skill writes
  image prompts as text only and never generates images, and it writes in
  English rather than Chinese.
- Narrowed `allowed-tools` to `Read`, so it cannot write image files.
- Rewrote `agents/openai.yaml` to match, in English.
- Prepended an English summary to the `description` frontmatter.

Everything else — the 小黑 character definition, the style DNA, the reference
files and the example masters — is unmodified. The upstream name and this
attribution are retained per the notice below.

The bundled example images in `examples/images/` and `ian-xiaohei-scenes/assets/examples/` were generated by Ian and are included as template masters and style calibration examples for this skill.

The recurring character "小黑" is part of Ian's visual language for Chinese article illustrations. When redistributing or adapting this repository, please keep the `Ian Xiaohei Scenes` name or provide attribution to Ian in your derived documentation.

`07-long-scroll-story-master.png` intentionally includes Ian's personal story labels and recognizable third-party names as a long-scroll quality master. Downstream users can keep it as a style reference, or replace the facts with their own story nodes when generating new images.

Author links:

- GitHub: <https://github.com/helloianneo>
- Website: <https://www.ianneo.xyz/opc>
- X/Twitter: <https://x.com/ianneo_ai>

